/**
 * Configurações de Ambiente - Produção
 * Este arquivo é usado em produção (deploy no Vercel)
 */

export const environment = {
  production: true,
  apiUrl: 'https://cardapio-api.onrender.com/api'
};
