/**
 * Configurações de Ambiente
 * Este arquivo é usado para configurar URLs e variáveis de ambiente
 */

export const environment = {
  production: false,
  apiUrl: 'https://cardapio-server.onrender.com/api'
};
